def multiple_args(
    name: str,
    age: int,
    active: bool = True,
) -> None:
    """Create a user.

    Args:
        name (str): The user's name.
        age (int): The user's age.
        active (bool): Whether the user is active.
    """
