def add(a: int, b: int) -> int:
    """Add numbers.

    This implementation intentionally stays simple.

    Args:
        a (int): First number.
        b (int): Second number.

    Returns:
        int: The result.
    """
    return a + b
