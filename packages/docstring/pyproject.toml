[project]
name = "gyomu-docstring"
dynamic = ["version"]
description = "Gyomu docstring"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "gyomu-schema",
    "gyomu-infra",
    "gyomu-python-analysis",
    "gyomu-ai-compiler",
    "returns",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"


[tool.uv.sources]
gyomu-schema = { workspace = true }
gyomu-infra = { workspace = true }
gyomu-python-analysis = { workspace = true }
gyomu-ai-compiler = {workspace=true}

[tool.hatch.version]
path = "../../version/__about__.py"
