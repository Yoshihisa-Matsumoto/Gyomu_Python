[project]
name = "gyomu-python"
version = "0.1.0"
description = "Enterprise operational framework"
requires-python = ">=3.14"
authors = [
    { name = "Yoshihisa Matsumoto", email = "yoshmatsumoto@gmail.com" }
]
readme = "README.md"

[tool.uv.workspace]
members = [
    "packages/schema",
    "packages/infra",
]

[dependency-groups]
dev = [
    "pytest>=9,<10",
]
